{
  "enabled": true,
  "name": "E2E Smoke Test on Screen Changes",
  "description": "specのタスク完了時に、画面に関係するファイル（.tsx）の変更が含まれていた場合のみ、ビルドしてPlaywrightのスモークテストを実行する。ロジックや型定義のみの変更ではスキップする。",
  "version": "1",
  "when": {
    "type": "postTaskExecution"
  },
  "then": {
    "type": "askAgent",
    "prompt": "タスクが完了しました。このタスクで画面に関係するファイル（src/ 配下の .tsx ファイル、ただし .test.tsx は除く）を変更した場合のみ、以下を実行してください：\n1. `npm run build` でビルド\n2. `npm run test:e2e` でPlaywrightスモークテストを実行\n3. テストが失敗した場合は結果を報告\n\n画面に関係するファイルを変更していない場合（型定義、ロジック、テストファイルのみの変更など）はスキップしてください。"
  }
}